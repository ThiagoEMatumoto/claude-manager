# Deps do sidecar REAL de transcrição + diarização (sidecar.py).
#
# torch NÃO está aqui: precisa do índice CUDA 12.8 (cu128) para a RTX 5070 Laptop
# (Blackwell, sm_120) e é instalado separadamente pelo scripts/setup-meeting-sidecar.sh
# via `--index-url https://download.pytorch.org/whl/cu128`. Pinar torch num
# requirements comum quebraria o resolver para o índice cu128.
#
# torchaudio NÃO é mais instalado: o faster-whisper não precisa dele (usa
# ctranslate2 + soundfile), e a diarização migrou de pyannote.audio para
# sherpa-onnx (ONNX Runtime puro, sem torch/torchaudio). torchaudio 2.11 (exigido
# pela GPU Blackwell/cu128) é incompatível com pyannote.audio 3.4 — esse conflito
# foi o motivo da migração.
#
# faster-whisper traz ctranslate2 (runtime de inferência). Versão recente é
# necessária para wheels compatíveis com CUDA 12.x.
# Versões EXATAS testadas (reprodutibilidade — pisos >= deixavam o resolver
# puxar builds não-validadas).
faster-whisper==1.2.1
soundfile==0.14.0
numpy==2.4.6
#
# sherpa-onnx = diarização (quem falou) via OfflineSpeakerDiarization. Lib
# ISOLADA (ONNX Runtime, sem torch/torchaudio) → não conflita com o torch cu128
# do faster-whisper. Substituiu pyannote.audio, que (1) quebra com torchaudio
# 2.11 (removeu AudioMetaData) e (2) é GATED no Hugging Face (exige token).
#
# Os 2 modelos ONNX da diarização (segmentation pyannote-3.0 + embedding TitaNet)
# são LIVRES (sem token) e baixados pelo scripts/setup-meeting-sidecar.sh para
# ~/.claude-manager/meeting-sidecar/models/. SEM os modelos, o sidecar NÃO trava:
# emite um `error` claro de diarização e cai no comportamento sem speaker.
# Diarização é OPCIONAL.
sherpa-onnx==1.13.3
